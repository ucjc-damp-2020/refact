def suma(a,b):
    return a+b

def multiplicaFactorial(a,b):
    c = suma(b,1)
    return a*c

def factorial(n):
    res = 1 #Asingamodvsfdkvhnfdkjhvlk
    for factor in range(n):
        res = multiplicaFactorial(res,factor)
    return res


def calculaAreaPerim(alto,ancho):
    perimetro = 2 *(alto*ancho)
    print('perimertro= ' + str(perimetro))
    area = alto*ancho

    print('Area= ' + str(area))


def modificador(input, modificador):
    valorEntrada = input
    return suma(valorEntrada, modificador)


class Proyecto:
    self.participantes = [Persona()]

class Persona:
    self.id
    def participante(proyecto):
        for participante in len(proyecto.participantes):
            if participante == id:
                return True
        return False

class Proyecto:
    self.participantes = [Persona()]
    def participante(inputPersona):
        for persona in len(self.participantes):
            if persona.id == inputPersona.id:
                return True
        return False

class Persona:
    self.id




def Precio(usuario, precio):
    if isPrecioEspecial(usuario):
        total = precio * (1 - 0.10)
    else:
        total = precio

    enviar(total)






factorial(3)


print("hola")

print('FIN')